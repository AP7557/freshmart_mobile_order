# Mobile App — NativeWind + React Native Reusables Setup

Run these commands inside mobile-app/:

```bash
npx expo install nativewind tailwindcss react-native-reanimated react-native-safe-area-context
npx expo install @react-native-async-storage/async-storage
npm install class-variance-authority clsx tailwind-merge lucide-react-native
```

Then follow the file changes in this zip.
